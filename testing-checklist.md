# Manual Tests

Legend: [Test to Run] | (Expected Outcome)

## Test Form Submission
* Select a mood | Selected mood is highlighted
* Fill optional fields | Optional field forms can be filled out
* Add activity tags one at a time | User can add activity tags individually 
* Add activity tags as a list | User can add multiple tags as a comma separated list
* Click "Save Entry" | Entry saves and appears with the information entered in the form